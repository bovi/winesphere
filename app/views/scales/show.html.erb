<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @scale.name %>
</p>

<p>
  <strong>Booth:</strong>
  <%= @scale.booth_id %>
</p>

<p>
  <strong>Offset:</strong>
  <%= @scale.offset %>
  <%= link_to 'Adjust Offset', scale_offset_path(@scale) %>
</p>

<p>
  <strong>Calibration:</strong>
  <%= @scale.calibration %>
</p>

<%= link_to 'Edit', edit_scale_path(@scale) %> |
<%= link_to 'Back', scales_path %>
