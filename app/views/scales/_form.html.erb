<%= form_with(model: scale, local: true) do |form| %>
  <% if scale.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(scale.errors.count, "error") %> prohibited this scale from being saved:</h2>

      <ul>
        <% scale.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :booth_id %>
    <%= form.text_field :booth_id %>
  </div>

  <div class="field">
    <%= form.label :offset %>
    <%= form.text_field :offset %>
  </div>

  <div class="field">
    <%= form.label :calibration %>
    <%= form.text_field :calibration %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
